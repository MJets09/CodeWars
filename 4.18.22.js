function betterThanAverage(classPoints, yourPoints) {
    let average = classPoints.reduce((a, b) => a + b) / classPoints.length

    return average < yourPoints ? true : false

}






//What was the part that you was stuck on?
//Had to figure out how to obtain the average of the array than compare to my points.


//What did you learn?
//The reduce method, still don't have it completely comprehended. Must work on more, added to Anki